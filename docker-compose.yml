networks:
  traefik-network:
    external: true

services:
  web:
    build:
      context: .
    container_name: ${SERVICE}
    environment:
      - APP_PASSWORD_HASH=${APP_PASSWORD_HASH}
      - SESSION_SECRET=${SESSION_SECRET}
      - DATA_DIR=/data
      - PORT=${PORT}
    volumes:
      # Host-Verzeichnis (kein Docker-Volume): SQLite + Thumbnails liegen hier.
      - ./data:/data
    # --- Hardening ---
    # Kein read_only: chromedp/Chromium braucht schreibbare Temp-/Profilpfade.
    security_opt:
      - no-new-privileges:true
    mem_limit: 1g
    pids_limit: 512
    restart: unless-stopped
    networks:
      - default
      - traefik-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${SERVICE}.entrypoints=http"
      - "traefik.http.routers.${SERVICE}.rule=Host(`${HOST}`)"
      - "traefik.http.middlewares.${SERVICE}-https-redirect.redirectscheme.scheme=https"
      - "traefik.http.routers.${SERVICE}.middlewares=${SERVICE}-https-redirect"
      - "traefik.http.routers.${SERVICE}-secure.entrypoints=https"
      - "traefik.http.routers.${SERVICE}-secure.rule=Host(`${HOST}`)"
      - "traefik.http.routers.${SERVICE}-secure.tls=true"
      - "traefik.http.routers.${SERVICE}-secure.tls.certresolver=default"
      - "traefik.http.routers.${SERVICE}-secure.service=${SERVICE}"
      - "traefik.http.services.${SERVICE}.loadbalancer.server.port=${PORT}"
      - "traefik.docker.network=traefik-network"
